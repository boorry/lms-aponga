# Contexte minimal par mission

Claude Code ne doit pas charger aveuglément tous les documents à chaque petite tâche.

## Foundation
Architecture + déploiement + stratégie de test + stack (`09_ENVIRONMENT/STACK_TECHNIQUE.md`) + `01_ORCHESTRATION/07_REPO_LAYOUT.md`.

## Data Model
*(section manquante ajoutée lors de l'audit final — cette mission précède désormais Identity, voir `00_MASTER_ORCHESTRATION.md`)*
Architecture + modèle de données + états + invariants.

## Identity
Architecture + sécurité + modèle de données + invariants + API + backlog.

## Learning Design
Architecture + modèle + états + versionnement + API + backlog.

## Media & Learning Delivery
Architecture + media lifecycle + sécurité + API + jobs + backlog + contraintes terrain (mode économie de données, cache PWA).

## Enrollment
Architecture + états + versionnement + sécurité + API + backlog.

## Submission
Architecture + états + sécurité + media + jobs + API + tests + backlog.

## Guardian
Architecture + états + sécurité + modèle + API + backlog.

## Frontend
Architecture + API + catalogue écrans + template source + stratégie de test.

## Deployment
Architecture + sécurité + jobs + déploiement + stack + validation.

Toujours relire `15_GLOSSAIRE_ET_FAQ.md` si un terme métier est ambigu.
